<%= stylesheet_link_tag 'application', media: 'all', :"data-turbolinks-track" => true %>
<%= csrf_meta_tags %>

<h1>Portfolio Analyzer</h1>
<%= params.inspect %>

<%= form_tag("/analyzer/analyze_group", method: "post") do %>
    <p>
      <%= select_tag(:groupid, options_for_select(@group_options)) %>
    </p>
    <p>
      <%= submit_tag('Select')%>
    </p>
<% end %>